# minimum versions of package dependencies for installation
# these should match the minimum versions specified in pyproject.toml
pynwb==2.5.0
hdmf==3.11.0  # required for bug fixes for generating some classes
